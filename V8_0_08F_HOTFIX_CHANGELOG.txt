V8.0.08f HOTFIX

Scope
- Targeted fix in core/job_launch.py only.
- No changes to Celery, Redis, UI, KPI, or job_recovery.py.

Change
- launch_processing_job() now:
  1. checks for an active job,
  2. runs recover_jobs() only for that single active job,
  3. uses the same settings-based thresholds as scheduled cleanup,
  4. re-queries the DB,
  5. blocks only if an active job still remains.

Intent
- Make test_launch_processing_job_recovers_obvious_stale_active_job_before_launching pass
  without reintroducing the broad inline recovery that broke jobs robustness tests.
