V7.0.87 — Sprint P0.1-bis

Objectif
- finir le nettoyage du lancement des jobs depuis les vues
- supprimer les dispatch Celery directs dans core/views.py
- rendre le cycle qualité réellement exécutable

Livré
- remplacement des `.delay()` restants dans `core/views.py` par `launch_processing_job(...)` ou `dispatch_task_after_commit(...)`
- `compute_metrics_all_job_task` accepte désormais `job_id` pour réutiliser le ProcessingJob existant
- nouveau helper `dispatch_task_after_commit(...)` pour les tâches non trackées lancées depuis les vues
- correction de compatibilité Django sur `CheckConstraint(condition=...)` afin de débloquer `manage.py test`
- tests job launch renforcés et rendus déterministes
- test d'audit projet : `views_direct_delay == 0`

Résultat audit
- avant : 24 violations (11 `views_direct_delay`, 13 `exports_direct_ws_append`)
- après : 13 violations (0 `views_direct_delay`, 13 `exports_direct_ws_append`)

Reste à traiter
- P0.2 : unifier `recover_jobs` et `cleanup_stale_processing_jobs_task`
- P1.2 : nettoyer les `ws.append(...)` restants côté exports
